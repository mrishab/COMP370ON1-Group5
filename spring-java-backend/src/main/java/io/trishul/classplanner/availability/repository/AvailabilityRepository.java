package io.trishul.classplanner.availability.repository;

import io.trishul.classplanner.availability.model.Availability;
import io.trishul.classplanner.common.repository.BaseRepository;

public interface AvailabilityRepository extends BaseRepository<Availability, Long> {
}

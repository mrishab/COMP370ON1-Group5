<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/img_classplanner_front"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/img_classplanner_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClassPlanner"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.register.RegisterActivity" />

        <activity android:name=".MainActivity" />

        <activity
            android:name=".CreateNewClassPlanActivity"
            android:theme="@style/Theme.ClassPlanner"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".SubmitCreateNewClassPlanActivity"
            android:theme="@style/Theme.ClassPlanner"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask" />

        <activity 
            android:name=".UploadGradPlanActivity"
            android:theme="@style/Theme.ClassPlanner"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>